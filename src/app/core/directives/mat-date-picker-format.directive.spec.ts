import { MatDatePickerFormatDirective } from './mat-date-picker-format.directive';

describe('MatDatePickerFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDatePickerFormatDirective();
    expect(directive).toBeTruthy();
  });
});
